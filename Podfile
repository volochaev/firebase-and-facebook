# Uncomment this line to define a global platform for your project

platform :ios, '8.0'
use_frameworks!

target 'firebase-example' do
    pod 'Firebase', '>= 2.4.2'
    pod 'Alamofire', '~> 3.0'
end

